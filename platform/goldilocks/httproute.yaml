apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: goldilocks
  namespace: goldilocks
  annotations:
    argocd.argoproj.io/ignore-healthcheck: "true"
spec:
  parentRefs:
    - name: shared
      namespace: gateway
  hostnames:
    - goldilocks.192.168.1.27.nip.io
  rules:
    - backendRefs:
        - name: goldilocks-dashboard
          port: 80
