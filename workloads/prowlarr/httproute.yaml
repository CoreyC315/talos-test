# Gateway API route (this cluster uses Gateway API, not Ingress) → prowlarr.192.168.1.27.nip.io.
# Replaces the homelab's nginx Ingress (prowlarr.local/.lan). Attaches to the shared Gateway.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prowlarr
  namespace: media
  annotations:
    argocd.argoproj.io/ignore-healthcheck: "true" # Gateway arrives in an earlier wave; don't gate
spec:
  parentRefs:
    - name: shared
      namespace: gateway
  hostnames:
    - prowlarr.192.168.1.27.nip.io
  rules:
    - backendRefs:
        - name: prowlarr
          port: 9696
