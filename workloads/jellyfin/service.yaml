apiVersion: v1
kind: Service
metadata:
  name: jellyfin
  namespace: media-gpu
spec:
  selector: { app: jellyfin }
  ports:
    - { name: http, protocol: TCP, port: 8096, targetPort: 8096 }
    - { name: http-80, protocol: TCP, port: 80, targetPort: 8096 } # seerr (migrated) uses :80
